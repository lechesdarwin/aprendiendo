+ Obtener un poco mas de informacion
	+ curl url -i
+ Guardar la salida 
	+ curl -o savefilename.extension url
+ Enviar metodo
	+ curl -X METHOD_HTTP 
+ Enviar encabezado 
	+ curl -H "clave:valor" url
+ 
